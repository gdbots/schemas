<?xml version="1.0" encoding="UTF-8" ?>
<pbj-schema xmlns="http://gdbots.io/pbj/xsd"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://gdbots.io/pbj/xsd http://gdbots.io/pbj/xsd/schema.xsd">

  <schema id="pbj:gdbots:contexts::app:1-0-0">
    <fields>
      <field name="_id" type="uuid" use-type-default="false"/>
      <field name="vendor" type="string" pattern="^[\w\.-]+$" max="50"/>
      <field name="name" type="string" pattern="^[\w\.-]+$" max="50"/>
      <field name="version" type="string" pattern="^[\w\.-]+$" max="20"/>
      <field name="build" type="string" pattern="^[\w\.-]+$" max="50"/>
      <field name="variant" type="string" pattern="^[\w\.-]+$" max="20"/>
    </fields>

    <php-options>
      <insertion-points>
        <imports>
          <![CDATA[
use Gdbots\Pbj\WellKnown\MessageRef;
            ]]>
        </imports>
        <methods>
          <![CDATA[
public function generateMessageRef(?string $tag = null): MessageRef
{
    return new MessageRef(self::schema()->getCurie(), $this->fget(self::_ID_FIELD) ?: $this->generateEtag(), $tag);
}

public function getUriTemplateVars(): array
{
    return [
      '_id' => $this->fget(self::_ID_FIELD),
      'vendor' => $this->fget(self::VENDOR_FIELD),
      'name' => $this->fget(self::NAME_FIELD),
      'version' => $this->fget(self::VERSION_FIELD),
      'build' => $this->fget(self::BUILD_FIELD),
      'variant' => $this->fget(self::VARIANT_FIELD),
    ];
}
            ]]>
        </methods>
      </insertion-points>
    </php-options>

    <js-options>
      <insertion-points>
        <imports>
          <![CDATA[
import MessageRef from '@gdbots/pbj/MessageRef';
            ]]>
        </imports>
        <methods>
          <![CDATA[
/**
 * @param {?string} tag
 * @returns {MessageRef}
 */
generateMessageRef(tag = null) {
  const id = this.get('_id') || this.generateEtag();
  return new MessageRef(this.schema().getCurie(), `${id}`, tag);
}

/**
 * @returns {Object}
 */
getUriTemplateVars() {
  return {
    _id: `${this.get('_id', '')}`,
    vendor: this.get('vendor'),
    name: this.get('name'),
    version: this.get('version'),
    build: this.get('build'),
    variant: this.get('variant'),
  };
}
            ]]>
        </methods>
      </insertion-points>
    </js-options>
  </schema>
</pbj-schema>
